// Package schema provides field-level validation for portwatch alert events.
// It ensures events conform to expected structure before being passed
// to downstream consumers such as audit writers or export handlers.
package schema
